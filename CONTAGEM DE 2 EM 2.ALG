algoritmo "contagem de 2 em 2"

var

Contador : inteiro

inicio

contador <- 0

Enquanto (contador <= 998) faca
escreval ( contador)

contador <- contador + 100,0

FimEnquanto

EscrevaL("A contagem foi de", contador)

fimalgoritmo